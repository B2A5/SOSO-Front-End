name: PR Preview

on:
  pull_request:
    branches: [develop] # PR 생성/업데이트 시 Vercel Preview 배포

jobs:
  vercel-preview:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Install Vercel CLI & PNPM
        run: npm install -g vercel@latest pnpm

      - name: Deploy to Vercel Preview
        id: deploy
        run: |
          # monorepo 내 apps/web에서 빌드하도록 cwd 지정
          vercel deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }} --cwd apps/web --yes

      - name: Comment PR with Preview URL
        uses: thollander/actions-comment-pull-request@v2
        with:
          message: |
            ✅ PREVIEW ${{ steps.deploy.outputs.preview_url }}
